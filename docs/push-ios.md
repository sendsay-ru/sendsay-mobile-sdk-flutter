---
title: iOS Push-уведомления
excerpt: Включение push-уведомлений в вашем приложении с помощью iOS SDK
slug: flutter-sdk-push-ios
categorySlug: integrations
parentDocSlug: flutter-sdk-push-notifications
---

Flutter SDK использует [нативный iOS SDK](https://github.com/sendsay-ru/sendsay-mobile-sdk-ios) для обработки push-уведомлений на устройствах iOS. В этом разделе описаны основные шаги настройки push-уведомлений в контексте Flutter SDK. 

Подробные инструкции и расширенные настройки смотрите в [документации по push-уведомлениям для iOS SDK](https://github.com/sendsay-ru/sendsay-mobile-sdk-ios/blob/main/docs/push-notifications.md).

> 📘
>
> Инструкции по созданию push-уведомлений в веб-приложении CDP Sendsay смотрите в разделе документации [Мобильные push-уведомления](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F).

> ❗️ Устаревание автоматических push-уведомлений
>
> Предыдущие версии SDK использовали method swizzling для автоматической регистрации push-уведомлений, что вызывало конфликты. Сейчас автоматическая регистрация не используется. Иногда это вызывало проблемы и поэтому больше не поддерживается. Реализуйте методы делегата вручную — [Методы делегата приложения](#шаг-2-методы-делегата-приложения).

## Требования

Чтобы отправлять push-уведомления из CDP Sendsay на устройства iOS:

- получите ключ Apple Push Notification service (APNs);
- добавьте и настройте интеграцию Apple Push Notification Service в веб-приложении CDP Sendsay.

> 📘
>
> Если вы ещё не выполнили эти шаги, следуйте инструкции [Настройка Apple Push Notification Service](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push/#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F).

## Интеграция

Ниже описаны минимальные необходимые настройки для включения push-уведомлений в вашем приложении.

### Шаг 1: Включение возможностей push

В Xcode откройте **Signing & Capabilities** для таргета приложения и включите:

- **Push Notifications** — для push-уведомлений с предупреждениями.
- **Background Modes** > **Remote notifications** — для тихих push-уведомлений.
- **App Groups** — для работы расширений уведомлений. Создайте новую группу приложений для вашего приложения.

> ❗️
>
> Для включения `Push Notifications` нужна платная учётная запись разработчика Apple.


### Шаг 2: Методы делегата приложения

Для корректной обработки push-уведомлений приложение должно реализовать ряд методов `AppDelegate`. 

Подробнее — в [документации по нативному SDK iOS](push-ios.md#шаг-3-настройте-группу-приложений).

Flutter SDK предоставляет базовую реализацию этих методов в классе [`SendsayFlutterAppDelegate`](https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter/blob/main/ios/Classes/SendsayFlutterAppDelegate.swift). Рекомендуем расширить `SendsayFlutterAppDelegate` в вашем `AppDelegate`.

Откройте `AppDelegate.swift` и замените его содержимое следующим:

```swift
import Foundation
import Flutter
import sendsay

@UIApplicationMain
@objc class AppDelegate: SendsayFlutterAppDelegate {

    override func application(
        _ application: UIApplication,
        didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
    ) -> Bool {
        GeneratedPluginRegistrant.register(with: self)
        return super.application(application, didFinishLaunchingWithOptions: launchOptions)
    }
}
```
Если вы не хотите или не можете расширить `SendsayAppDelegate`, можете использовать его в качестве справочника для самостоятельной реализации необходимых методов делегата.

### Шаг 3: Настройка группы приложений

При [инициализации SDK](../docs/setup.md#инициализируйте-sdk) необходимо указать группу приложений, созданную в [шаге 1](#шаг-1-включение-возможностей-push). Для этого установите свойство `appGroup`:

```dart
import 'package:sendsay/sendsay.dart';

final _plugin = SendsayPlugin();
_plugin.configure(SendsayConfiguration(
  // ...
  ios: IOSSendsayConfiguration(
    appGroup: 'your app group',
  ),
));
```

### Шаг 4: Запрос разрешения на отправку уведомлений

Для отправки видимых push-уведомлений приложению требуется явное разрешение от пользователя.

Вы можете запросить разрешение, вызвав `SendsayPlugin().requestPushAuthorization()`:

```dart
_plugin.requestPushAuthorization()
.then((accepted) => print("User has ${accepted ? 'accepted': 'rejected'} push notifications."))
.catchError((error) => print('Error: $error'));
```

> 👍
>
> Статус разрешения сохраняется как свойство клиента `apple_push_notification_authorized`.

### Контрольный список:

- [ ] CDP Sendsay теперь может отправлять push-уведомления на устройства iOS. Подробнее — в руководстве [Создание нового уведомления](https://docs.sendsay.ru/other-channels/mobile-push/how-to-create-mobile-push-campaign).
- [ ] Поддержка изображений и кнопок в уведомлениях пока не включена. Для включения смотрите инструкцию по настройке [расширенных push-уведомлений](#расширенные-push-уведомления-в-разработке).

## Настройка

### Расширенные push-уведомления (в разработке)

Богатые push-уведомления могут содержать изображения, кнопки действий и аудио. Чтобы включить эту функциональность, добавьте в проект два расширения приложения: **Notification Service Extension** и **Notification Content Extension**.

Для каждого расширения следуйте инструкциям в разделе [Расширения уведомлений](https://documentation.bloomreach.com/engagement/docs/ios-sdk-notification-extensions) документации по нативному iOS SDK.

Метод `SendsayNotificationContentService.didReceive()` улучшает содержимое уведомления, добавляя изображение и действия, переданные в полезной нагрузке `UNNotification`. Все показанные действия автоматически регистрируются с нужной конфигурацией: SDK обеспечит корректное открытие приложения с переданными данными и обработает клики по кампаниям.

#### Контрольный список:
- [ ] Убедитесь, что push-уведомления с изображениями и кнопками, отправленные из CDP Sendsay, корректно отображаются на устройстве. Отслеживание доставки push-уведомлений должно работать.
- [ ] Если кнопки не появляются в развёрнутом уведомлении, значит, расширение контента не применяется. Проверьте значение `UNNotificationExtensionCategory` в `Info.plist` (оно должно быть внутри `NSExtensionAttributes`) и убедитесь, что `iOS Deployment Target` совпадает у расширений и у основного приложения.

### Получение токена push-уведомления вручную

Иногда приложению требуется получить актуальный push-токен во время работы. Для этого используйте метод `Sendsay.shared.trackPushToken`.

Вызов метода `anonymize` удалит токен push-уведомлений из локального хранилища. После анонимизации необходимо заново получить push-токен и передать его в SDK — сразу после anonymize или после последующей идентификации клиента.

``` swift
Messaging.messaging().token { token, error in
  if let token = token {
    SwiftSendsayPlugin.handlePushNotificationToken(deviceToken: token)
    resolve(nil)
  } else {
    reject(error)
  }
}
```

### Показать уведомления на переднем плане

Если push-уведомление приходит, когда приложение находится в активном состоянии, вы можете управлять его отображением, переопределив следующий метод:

```swift
    override func userNotificationCenter(_ center: UNUserNotificationCenter, willPresent notification: UNNotification, withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
        // show notification even if the app is in the foreground
        if #available(iOS 14, *) {
            completionHandler([.banner])
        } else {
            completionHandler([.alert])
        }
    }
```
