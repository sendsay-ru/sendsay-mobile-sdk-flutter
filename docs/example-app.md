---
title: Example app
excerpt: Build, run, and navigate the example app included with the Flutter SDK
slug: flutter-sdk-example-app
categorySlug: integrations
parentDocSlug: flutter-sdk
---

Flutter SDK включает пример приложения, который можно использовать как эталонную реализацию. Вы можете собрать и запустить его, протестировать функции CDP Sendsay и сравнить свой код с ожидаемым поведением и кодом в примере.

## Требования

Для сборки и запуска примера приложения потребуется:
- [Flutter SDK](https://docs.flutter.dev/tools/sdk)
- [Git](https://git-scm.com/)
- [Android Studio](https://developer.android.com/studio) с виртуальным или физическим Android-устройством.
- [Xcode](https://developer.apple.com/xcode/) и [CocoaPods](https://cocoapods.org/) с виртуальным или физическим iOS-устройством.

> 👍
>
> Если вы только начинаете работать с Flutter, ознакомьтесь с руководством «Начало работы» в официальной документации Flutter: https://docs.flutter.dev/get-started/install.

## Сборка и запуск примера приложения

1. Клонируйте репозиторий [sendsay-flutter-sdk](https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter) на GitHub:

   ```shell
   git clone https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter.git
   ```
2. Перейдите в каталог с примером приложения `example`:

   ```shell
   cd sendsay-flutter-sdk/example
   ```

### Запуск на iOS

1. В CocoaPods выполните команду:

   ```shell
   cd ios
   pod install
   cd ..
   ```
2. Подключите виртуальное или физическое iOS-устройство.
3. Запустите приложение:

   ```shell
   flutter run
   ```

### Запуск на Android

1. Подключите виртуальное или физическое Android-устройство.
2. Запустите приложение с нужным flavor:

   ```shell
   flutter run --flavor gms
   ```

   Для устройств Huawei без Google Play Services используйте:

   ```shell
   flutter run --flavor hms
   ```

   > 👍
   >
   > Если вы используете Visual Studio Code, вы можете создать отдельную [конфигурацию запуска](https://code.visualstudio.com/docs/editor/debugging#_launch-configurations) с указанием flavor.

> 📘
>
> Чтобы протестировать push-уведомления, настроите интеграцию через [Firebase](https://documentation.bloomreach.com/engagement/docs/android-sdk-firebase) или [Huawei](https://documentation.bloomreach.com/engagement/docs/android-sdk-huawei) в веб-приложении Sendsay.

## Навигация по примеру приложения

![Экраны приложения-примера (iOS)](https://raw.githubusercontent.com/sendsay-ru/sendsay-mobile-sdk-flutter/main/docs/img/example-app-flutter-ios.png)

После запуска приложения в симуляторе вы увидите экран **ConfigPage**. На нём необходимо указать:

- ID проекта,
- API-ключ,
- базовый URL API.

Эти параметры соответствуют [настройкам подключения](https://docs.sendsay.ru/other-channels/mobile-push/how-to-connect-mobile-push#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BF%D0%BE%D0%B4%D0%BA%D0%BB%D1%8E%D1%87%D0%B5%D0%BD%D0%B8%D1%8F) мобильного приложения в CDP Sendsay. 

При необходимости можно отключить автоматическое отслеживание сессий (по умолчанию включено). После этого нажмите «Настроить», чтобы [инициализировать SDK](../docs/setup.md#инициализируйте-sdk)

> Исходный код экрана: [`config.dart`](https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter/blob/main/example/lib/page/config.dart)

На **главном экране** есть несколько разделов и кнопок для тестирования различных функций SDK:

> Исходный код экрана: [`home.dart`](https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter/blob/main/example/lib/page/home.dart)

- **Push events** — запрос разрешения на push-уведомления и проверка их настройки.

- **Customer** — работа с профилем клиента: 
   - получение soft ID (cookie), 
   - идентификация клиента (используется тестовый email test-user-1@test.com),
   - анонимизация клиента (`Anonymize`).

- **Fetch** — получение согласий и рекомендаций клиента.

- **Flush Mode** — получение и установка режима отправки данных, а также ручной запуск отправки.

- **Flush Period** настройка периода отправки данных.

- **Track** отслеживание тестового события, а также ручное отслеживание начала и окончания сессии при отключённом автоматическом трекинге.

- **Trigger in-app messages by event**  отправка тестовых событий для запуска встроенных сообщений:
    - `Modal` (отслеживание события `test_msg_modal`)
    - `Fullscreen` (отслеживание события `test_msg_fullscreen`)
    - `Slide-in` (отслеживание события `test_msg_slide`)
    - `Alert` (отслеживание события `test_msg_alert`)

- **Log Level** — получение текущего уровня логирования.

- **Входящие сообщения приложения** — работа с входящими сообщениями приложения: загрузка сообщений и отметка их как прочитанных.

- «In App CB Example Page» — переход на экран **InAppCbPage** для тестирования блоков контента в приложении (функциональность в разработке) Используйте ID плейсхолжеров: `example_top`, `example_list`, `ph_x_example_iOS`, `ph_x_example_Android`.

  > Исходный код экрана: [in_app_cb_page.dart](https://github.com/sendsay-ru/sendsay-mobile-sdk-flutter/blob/main/example/lib/page/in_app_cb_page.dart)

После тестирования функций приложения вы можете найти профиль клиента в веб-приложении CDP Sendsay (раздел «Клиенты») и посмотреть события и свойства, отправленные SDK.

По умолчанию клиент отслеживается анонимно с использованием soft ID. Его значение можно найти в логах приложения и использовать для поиска соответствующего профиля.

Если была выполнена идентификация клиента, профиль будет доступен по тестовому email test-user-1@test.com.

![Примеры экранов приложения (Android)](https://raw.githubusercontent.com/sendsay-ru/sendsay-mobile-sdk-flutter/main/docs/img/example-app-flutter-android.png)

## Устранение неполадок

Если при запуске примера приложения возникают ошибки:

- в каталоге `example` выполните команды:
   ```
   flutter clean
   flutter pub get
   ```
- убедитесь, что при запуске на Android указан корректный flavor — `--flavor gms` или `--flavor hms`.