---
title: Push notifications
excerpt: Enable push notifications in your app using the Flutter SDK
slug: flutter-sdk-push-notifications
categorySlug: integrations
parentDocSlug: flutter-sdk
---

CDP Sendsay позволяет отправлять push-уведомления пользователям вашего мобильного приложения. Приложение получает push-сообщение, SDK обрабатывает его и отображает уведомление на устройстве клиента.

Push-уведомления также могут быть **тихими** — они используются для обновления интерфейса приложения или запуска фоновых задач без отображения уведомления пользователю.

> 📘
>
> Инструкции по созданию push-уведомлений в веб-приложении CDP Sendsay смотрите в разделе документации [Мобильные push-уведомления](https://docs.sendsay.ru/other-channels/mobile-push/how-to-create-mobile-push-campaign).

## Требования

Flutter SDK использует нативные механизмы платформ Android и iOS для обработки push-уведомлений.

Шаги по добавлению минимальной функциональности push-уведомлений (получение оповещений) в ваше приложение для каждой платформы описаны в отдельных разделах документации:

- [Push-уведомления Android](../docs/push-android.md)
- [Push-уведомления iOS](../docs/push-ios.md)

## Настройка

В этом разделе описаны настройки, которые можно использовать после интеграции базовой функциональности push-уведомлений.

### Настройка автоматического отслеживания push-уведомлений

По умолчанию SDK:
- автоматически отслеживает push-токены,
- отображает полученные уведомления,
- отслеживает событие campaign при доставке и открытии уведомления.

В [конфигурации SDK](../docs/configuration.md) можно задать частоту отслеживания push-токена с помощью свойства `pushTokenTrackingFrequency` (значение по умолчанию — `TokenFrequency.onTokenChange`).

На Android вы можете отключить автоматическое отслеживание push-уведомлений, установив свойство `automaticPushNotifications` в `false` в [конфигурации SDK для Android](../docs/configuration.md#параметры-конфигурации-специфичные-для-android).

>❗️
>
> В настоящее время Flutter SDK **не поддерживает** отключение автоматического отслеживания push-уведомлений в iOS.

### Реагирование на взаимодействия с push-уведомлениями

После выполнения шагов интеграции для каждой платформы приложение готово к приёму push-уведомлений.

Чтобы реагировать на действия пользователя (открытие приложения, переход по ссылке), настройте слушатель для `SendsayPlugin().openedPushStream`:

```dart
import 'package:sendsay/sendsay.dart';

final _plugin = SendsayPlugin();
final subscription = _plugin.openedPushStream.listen((openedPush) {
  switch(openedPush.action) {
    case PushActionType.app:
      // last push directed user to your app with no link

      // log data defined on Sendsay backend
      print('app - ${openedPush.data}'); 
      break;
    case PushActionType.deeplink:
      // last push directed user to your app with deeplink
      print('deeplink - ${openedPush.url}');
      break;
    case PushActionType.web:
      // last push directed user to web, nothing to do here
      print('web');
      break;
  }
});
```

Вызовите `subscription.cancel()`, когда слушатель больше не нужен.

Рекомендуем регистрировать слушатель как можно раньше. SDK сохраняет последнее push-уведомление и вызовет слушатель сразу после его регистрации.

> ❗️
>
> Для поддержки deep-links требуется дополнительная настройка на стороне платформы. Подробнее — в документации для соответствующих нативных платформ ([Android](../docs/push-android.md) и [iOS](../docs/push-ios.md)). В качестве альтернативы можно использовать действие **Open app** и добавить полезную нагрузку в `Additional data`.


### Реакция на полученние push-уведомления

Для обработки полученных push-уведомлений (в том числе тихих) можно использовать `SendsayPlugin.receivedPushStream`.

```dart
final subscription = _plugin.receivedPushStream.listen((receivedPush) {
  print(receivedPush);
});
```

Вызовите `subscription.cancel()`, когда слушатель больше не нужен.

Рекомендуем регистрировать слушатель как можно раньше. SDK сохраняет последнее push-уведомление и вызовет слушатель сразу после его регистрации.

> ❗️
>
> На Android слушатель вызывается как для обычных, так и для бесшумных push-уведомлений. На iOS — **только** для тихих push-уведомлений из-за технических ограничений.

### Ручное отслеживание push-уведомлений

Если вы отключили [автоматическое отслеживание push-уведомлений](#настройка-автоматического-отслеживания-push-уведомлений)(только Android) или используете push-уведомления от других поставщиков, вы можете отслеживать события вручную.

> ❗️
>
> В настоящее время Flutter SDK **не поддерживает** отключение автоматического отслеживания push-уведомлений на iOS.

#### Отслеживание push-токена (FCM)

Используйте метод `trackPushToken` для ручного отслеживания push-токена FCM:

```dart
SendsayPlugin().trackPushToken("382d4221-3441-44b7-a676-3eb5f515157f")
```

Метод немедленно отправляет токен в CDP Sendsay независимо от [конфигурации SDK](../docs/configuration.md) для `tokenTrackFrequency`.

#### Отслеживание доставленных push-уведомлений

Используйте метод `trackDeliveredPush`:

```dart
final Map<String, dynamic> payload = {
  "platform": "android",
  "subject": "subject",
  "type": "push",
  "url_params": {
    "utm_campaign": "Campaign name",
    "utm_medium": "mobile_push_notification",
    "utm_content": "en",
    ...
  },
  ...
};
SendsayPlugin().trackDeliveredPush(payload)
```

> ❗️
>
> На работу `trackDeliveredPush` может влиять функция согласия на отслеживание. Подробнее — в [документации по согласию на отслеживание](../docs/tracking-consent.md).

#### Отслеживание нажатых push-уведомлений

Используйте метод `trackClickedPush`:

```dart
final Map<String, dynamic> payload = {
  "platform": "android",
  "subject": "subject",
  "type": "push",
  "url_params": {
    "utm_campaign": "Campaign name",
    "utm_medium": "mobile_push_notification",
    "utm_content": "en",
    ...
  },
  ...
};
SendsayPlugin().trackClickedPush(payload)
```

> ❗️
>
> Поведение `trackClickedPush` также зависит от настроек согласия на отслеживание. Подробнее — в [документации по согласию на отслеживание](tracking-consent.md).

### Пользовательская обработка данных push-уведомлений

Если встроенные нативные методы `SendsayModule.Companion.handleRemoteMessage` (Android) и `SendsayNotificationService().process` (iOS) не подходят для вашего сценария, вы можете самостоятельно обрабатывать push-уведомления и их полезную нагрузку (payload).

Полезная нагрузка push-уведомлений формируется на стороне CDP Sendsay и может содержать данные для Android, iOS и веб-платформ. Payload имеет формат JSON и может быть достаточно сложным.

#### Пример payload

```json
{
    "notification_id": 123,
    "url": "https://example.com/main_action",
    "title": "Notification title",
    "action": "app|browser|deeplink|self-check",
    "message": "Notification message",
    "image": "https://example.com/image.jpg",
    "actions": [
        {"title": "Action 1", "action": "app|browser|deeplink", "url": "https://example.com/action1"}
    ],
    "sound": "default",
    "aps": {
        "alert": {"title": "Notification title", "body": "Notification message"},
        "mutable-content": 1
    },
    "attributes": {
        "event_type": "campaign",
        "campaign_id": "123456",
        "campaign_name": "Campaign name",
        "action_id": 1,
        "action_type": "mobile notification",
        "action_name": "Action 1",
        "campaign_policy": "policy",
        "consent_category": "General consent",
        "subject": "Subject",
        "language": "en",
        "platform": "ios|android",
        "sent_timestamp": 1631234567.89,
        "recipient": "user@example.com"
    },
    "url_params": {"param1": "value1", "param2": "value2"},
    "source": "xnpe_platform",
    "silent": false,
    "has_tracking_consent": true,
    "consent_category_tracking": "Tracking consent name"
}
```